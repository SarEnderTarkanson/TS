const activeUsers: string[] = [];

activeUsers.push("Alpy");

const ageList: number[] = [10, 15, 20];

const bools: Array<boolean> = [true, false];

//declaring custom types

type Point2 = {
  x: number;
  y: number;
};

const coords: Point2[] = [];

coords.push({ x: 23, y: 28 });
coords.push({ x: 23, y: 28 });

//declaring types for multidimensional arrays
const board: string[][] = [
  ["X", "O", "X"],
  ["O", "X", "O"],
  ["X", "O", "X"],
];

const demo: number[][][] = [[[1]]];

